# DreamWeaver AI

An AI-powered storytelling system that generates structured prompts and uses LLMs to create stories.

## Features
- Structured prompt generation
- Story generation using LLMs
- Custom dataset creation

## Project Structure
- src/ → core scripts
- data/raw → generated dataset
- data/processed → cleaned dataset

## Setup

```bash
pip install -r requirements.txt